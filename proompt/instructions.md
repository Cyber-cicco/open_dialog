THE TIME HATH COME

We'll implement the node creation. Modal was a bad idea, it's discarded. In order to do so, we will have to have a few buttons in the top left of the screen, displayed with an absolute position. We'll have one button for each type of node (Dialog, Choice, Phylum). Add some SVG components if necessary. Don't implement logic.



