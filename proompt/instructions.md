It feels like as the text files are stored in plain text to have git diffs on them, the rest of it, which is basically metadata, should be stored in SQLite and not on plain files. Since SQLite is basically a file, it should be versionned by git and work with it, no ?



