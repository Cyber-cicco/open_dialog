Make it so I when the last displayed node is a choice node, handleContinue choses the node from the choice that is currently focused. By default, the first choice should be focused. The user should be able to navigate the choices using the arrow keys.
